name: Deploy to GitHub Pages

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install
        working-directory: ./frontend
        run: npm install && run npm build

      - name: Deploy website
        uses: Jakejarvis/gh-pages-deploy-action@v4
        with:
          branch: pages
          folder: ./frontend/build
